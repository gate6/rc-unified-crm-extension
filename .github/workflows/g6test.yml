name: Deploy code to API server

on:
  push:
    branches:
      - test-gate6

jobs:
  preview:
    name: Deploy to Test Environment
    runs-on: runner-253.106

    # 👇 Add this block
    environment:
      name: test
    
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      # Creating API zip archive
      - name: Create zip archive
        run: |
          rm -rf api.zip
          zip -r api.zip ./*

      # Deploying api.zip to the API server
      - name: Copy file via SSH
        uses: appleboy/scp-action@master
        with:
          host: ${{ vars.API_HOST_TEST }}
          username: ${{ vars.SSH_USER_TEST }}
          key: ${{ secrets.SSH_USER_KEY_TEST }}
          port: ${{ vars.PORT }}
          source: "api.zip"
          target: "public_html"
          debug: true

      - name: Execute remote SSH commands
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ vars.API_HOST_TEST }}
          username: ${{ vars.SSH_USER_TEST }}
          key: ${{ secrets.SSH_USER_KEY_TEST }}
          port: ${{ vars.PORT }}
          script: |
            cd /home/rc-crmapi/public_html
            unzip -o api.zip
            rm -rf api.zip
            npm install
            pm2 stop src/server.js || true
            pm2 start src/server.js
